- When adding new methods to CacheService (for example deleteKey), update all Nest test CacheService mocks in crawling specs immediately to avoid provider/method mismatch regressions.
- Redis-backed queue persistence should avoid immortal empty keys: delete key when queue becomes empty instead of writing [].
- Queue persistence changes require rerunning crawling-scheduler, archive-orchestrator, and fault-isolation targeted tests together.
- api-isolation.spec.ts contains strict latency assertions (<10ms) that can be flaky on loaded machines; treat failures there as potential environment jitter unless coupled with functional regressions.

- When AI summary generation paths change, ensure cache merge only uses summary states reloaded from DB; otherwise `/notices/archive` can show cache-only summaries that disappear after restart.

- In immutable archive mode, verify placeholder methods are not left as no-ops (`updateSourceHtml`, `upgradePendingNotices`), otherwise metadata gaps (e.g., originalContent fields) persist indefinitely.

- Diffchain integrity rules must hash empty-string detail values ("") the same way as append-time logic; audit code should only treat literal null as no-hash.
- Keep canonical event hash generation in one place (`ChangeTrackingService.buildDiffEvent`); avoid duplicating manual hash payload assembly in archive service paths (genesis seed, explicit invalidation).

- Diffchain audit/event hash parity requires tracked-field type coercion when reconstructing from details: `num` must be number and `isDone` must be boolean (not string), both in chain-audit reconstruction and notice baseline-from-timeline merge.

- NSM source_deleted detection currently runs through proposalReason retry path only for rows with `contentId IS NULL`, `lifecycle_status='active'`, and snapshot `is_done=0`; if `is_done=1` first, deleted NSM detail pages can stay `active` unless another path appends `source_deleted`.
- Large apply_patch edits in `notice-archive.service.ts` can auto-correct into malformed control flow; after each sizable patch, immediately verify with focused diff + `get_errors` before continuing cross-file refactors.

- ProposalReason backfill must treat NSM detail-page HTTP 404 as `source_deleted` (invalidated) immediately, not only alert-message probes/timeouts; keep coverage in `archive-orchestrator.service.spec.ts`.

- Browser-driven crawling in NestJS now uses BrowserLeaseManagerService as the ownership boundary; new browser-owning methods should lease through it instead of reintroducing per-call guard logic.
- Immutable snapshot mode should persist archive integrity results in dedicated tables (checks/state) instead of mutating `notice_archives`; keep constructor optional deps appended to avoid breaking direct service instantiation in tests.
- AI summary backfill eligibility for NSM notices should use the latest `proposalReason` field record from diffchain, not the latest non-empty value; otherwise cleared/empty head states can be misread as recoverable.

- ArchiveSync now treats summary backfill + unavailable retry as one unified phase (`runSummaryBackfill`); update tests/calls to assert `retryScanned/recovered/stillFailed` on SummaryBackfillResult instead of invoking `runUnavailableRetry`.


- For `isDone` sync diff events, derive both before/after snapshots from the chain-head baseline and then toggle only `isDone`; mixing chain-head before with stale DB-row after can emit false non-isDone regressions (e.g., PAL -> NSM proposalReason rollback noise).

- Manage change-chain `eventType` via `CHANGE_EVENT_TYPE` in `notice-change-event.entity.ts` and reference it across API/service/tests to avoid drift between literal strings and unions.

- Screenshot display must come from the immutable archive snapshot contract, not from diffchain revision overlays; preserve a separate immutableSnapshot field in detail responses and keep UI consumers on that path.

- `isDone` change events are routed separately from generic change notifications: batch routing should split payloads with `changedFields.includes('isDone')` and send a dedicated notice-period-ended embed/digest.
- Targeted validation for notification routing changes: `npm test -- --runInBand src/modules/notification/notification.service.spec.ts src/modules/notification/notification-batch.service.spec.ts` from `backend/`.
- file.kiwi integration uses ESM-only `@file-kiwi/node`; in this CommonJS Nest backend, load it with runtime `await import(...)` inside methods rather than top-level static imports.

- Web push dispatch previously used full `Promise.all` fan-out; keep bounded concurrency + retry/backoff (429/5xx/network, Retry-After aware) to reduce burst-drop risk and stabilize delivery stats.
- When extending web push send semantics, validate with targeted tests: `npm run test -- web-push-notification.service.spec.ts notification-batch.service.spec.ts` in `backend/`.
- `pal-crawl` paging keys differ by source: PAL search uses `pageUnit`, but `NsmLmSts` pending/full list search uses `pageSize`; keep NSM full-sync helpers forcing `pageSize: 100` to avoid silent inefficiency or ignored query params.

- `ArchiveOrchestratorService.filterAlreadyArchivedNotices()` returns only new/unarchived notice nums; do not reuse it to select existing records for PAL/NSM recompare paths. Use `NoticeArchiveService.getExistingNoticeNumSet()` when the intent is existing-row refresh.


- `CRAWLING.BROWSER_MAX_CONCURRENCY` must be enforced by `BrowserLeaseManagerService` admission logic; if lease acquisition is hardcoded to a single active slot, page-level async crawl gains are erased by browser-stage serialization.

- Screenshot backfill was previously serialized by a single drain loop; bounded batch draining only helps if BrowserLeaseManagerService also admits more than one concurrent lease.

- Screenshot queue draining should split PAL and NSM batches: PAL can use bounded parallelism, but NSM capture should stay serialized within its own subgroup to avoid rate-limit bottlenecks while not blocking PAL throughput.

- Optional constructor injection of DiscordBridgeService inside DbMirror services can trigger Injector "undefined dependency" warnings under circular load order; prefer ModuleRef lazy resolution for bridge logging in db-mirror layer.
- Async apply workers must not drop entire batches on partial archive success: remove only confirmed-success keys, keep failed keys queued, and back off/retry on no-progress batches.
- Summary backfill DB writes now use bulk upsert (`updateSummaryStatesByNoticeNums`) and only remove queue keys for notice_nums confirmed persisted; keep single-row fallback path for older test doubles that do not implement the bulk method.
- Prevent NSM->PAL oscillation noise by filtering pending-recompare work to archive rows with `contentId IS NULL` both at staging and at apply-worker execution; stale queue items after restart should be dropped when already PAL-enriched.
- Summary backfill staging can bottleneck on proposalReason recovery N+1; use batched change-chain lookup (`getLatestFieldValues`) and keep a single-call regression test in notice-archive.service.spec.ts.


- In archive sync apply workers, avoid `return` from `finally`; enforce retry exhaustion by skipping reschedule branch and logging exhaustion, then verify with `get_errors` + fault-isolation filters.

- Bootstrap `waitForAsyncApplyIdle` must use phase-scoped workerSelection (pending/full/summary separately); default selection can make summary stage appear stuck while waiting unrelated async queues.

- NSM-origin routing based only on `notice_archives.contentId IS NULL` can reprocess PAL-enriched notices under immutable snapshots; guard with change-chain latest `contentId` (`getLatestFieldValues`) before staging/recompare.

- Snapshot-artifact backfill has no dedicated queue: only PAL `refreshExistingPalNoticesInBackground` (crawlAllPages full sweep) re-supplies html/sha/http for existing rows. `getNoticesWithMissingHtml()` and `backfillMissingScreenshots()` are dead (no callers), and `runIntegrityScan` is read-only. Rows dropped from source listings stay permanently deficient.

- `proposalReason` normalization must preserve line breaks (`\n`): avoid global `/\s+/g` collapse in archive/orchestrator paths, or multiline bodies from pal-crawl/NSM detail get flattened in DB and diffchain snapshots.

- Preserve proposalReason line breaks for storage/display, but use `canonicalizeProposalReasonForComparison` for change equality and retry verification so legacy flattened text and parser multiline text do not emit/requeue formatting-only changes.
- `refresh-existing-pending-bills` must select only pending-candidate notice numbers and then filter through `getArchivedNullContentIdNums`; otherwise non-pending or PAL-origin rows are repeatedly detail-crawled and can amplify false events.
- Diffchain canonicalization/tracked-field changes require a new `canon_version`; audit must replay each event with that version's immutable tracked-field list and comparison semantics. Never reconstruct historical events with today's `DEFAULT_TRACKED_FIELDS` or comparator.

- Once a notice's diffchain head has a non-null PAL `contentId`, later NSM (`contentId=null`) upserts must preserve PAL-authoritative identity, committee/contentCommittee, proposalReason, and detail metadata from the chain head; immutable DB rows may still contain the original NSM snapshot.

- `detectNsmDeletedBillFromHtml` (crawling-core.service.ts) structural heuristic can false-positive: opinion.lawmaking.go.kr appears to reuse the same "안건정보가 없습니다" + history.back() template for transient session/anti-bot/error conditions, not only truly deleted bills. Every call site that reacts to `NsmBillDeletedError` by calling `appendSourceDeletedAndFlushNotifications` MUST first re-confirm via an independent `probeNsmDeletedBillAlert` HTTP probe (separate from the Puppeteer capture) before marking `source_deleted` — a single Puppeteer-observed signal is not sufficient, since diffchain events are append-only/hard to reverse. Fixed in `archiveNsmBillItems` (initial NSM archive) and `probeExistingNsmBillsForSourceDeletion`; `fetchAndUpdateProposalReason` (backfill retry) already did this correctly and is the reference pattern. Tests: `archive-orchestrator.service.spec.ts`.
- All source_deleted detection/probe logging (in `probeExistingNsmBillsForSourceDeletion`, and the pending-sync source-missing-detection + NSM detail-probe blocks in `archive-sync-phase-executors.ts`/`crawling-scheduler-pending-support.ts`) now goes through `logAndBridge` (with `deps.discordBridge`/`this.discordBridge` and `archiveSyncLogger`/`deps.logger`) instead of plain `LoggerUtils`/`this.logger` calls, so deletion events are visible on the Discord debug bridge. Keep new deletion/probe logging on this pattern going forward.

- Found & fixed a 2nd Waitingroom-related false-positive gap in NSM deletion detection: `navigateWithWaitingroomBypass` does NOT throw when the Waitingroom retry budget is exhausted (only logs a warning and returns whatever page state exists) — `captureNsmDetailFull` previously ran `detectNsmDeletedBillFromHtml` against that leftover (possibly still-Waitingroom) HTML unconditionally. Fixed by: (1) new `isWaitingroomHtml(html)` string-based helper in `waitingroom-bypass.ts` (title-tag regex, mirrors `isWaitingroomPage`), used as an early bail in `detectNsmDeletedBillFromHtml` so the HTTP-probe path (`probeNsmDeletedBillAlert`) can never confirm deletion off Waitingroom HTML either; (2) `captureNsmDetailFull` now calls `isWaitingroomPage(page)` right after `navigateWithWaitingroomBypass` and throws a new `NsmWaitingroomUnresolvedError` (distinct from `NsmBillDeletedError`) before any deletion check/detail parsing runs — callers only react to `NsmBillDeletedError`, so this never triggers `source_deleted`. Tests: `crawling-core.service.spec.ts` (`browser lease coverage` + new `probeNsmDeletedBillAlert` describe block).

- **CRITICAL bug found via prod log analysis (2026-09-02)**: the `notice_archives` immutability trigger (`202608130001-allow-snapshot-artifact-first-fill.migration.ts`) listed `lifecycle_status`/`source_deleted_at` as fully-immutable columns, so `appendSourceDeletedEventByNoticeNum`'s `archiveRepository.update({lifecycleStatus:'source_deleted', sourceDeletedAt})` call ALWAYS aborted with `SQLITE_CONSTRAINT: notice_archives is immutable after initial snapshot archive` (silently caught/logged, never surfaced). The diffchain event still got appended (so change history/notifications look correct), but the raw row stayed `lifecycle_status='active'` forever — so every probe/list-reconciliation query kept re-discovering and re-"confirming deleted" the same bills forever (prod evidence: notice 2214911 re-confirmed-deleted via HTTP probe on two different days; notices 2200001-2200005, including the exact 2200004 the user originally reported, got hit by `markSourceDeletedByMissingNsmNums` in one burst). Fixed with new migration `202609020001-allow-lifecycle-status-transition.migration.ts`: removes lifecycle_status/source_deleted_at from `IMMUTABLE_COLUMNS` and adds a one-directional-transition predicate (`active` -> `source_deleted`|`renumbered`, source_deleted_at fillable once, no other transition allowed). Updated `archive-immutability-trigger.spec.ts` accordingly (was asserting the broken behavior as correct). **Operational follow-up needed**: after deploying this migration, existing rows that have a `source_deleted`/`renumbered` diffchain event but still show `lifecycle_status='active'` in `notice_archives` need a one-time reconciliation pass (no repair script written yet).

- **2nd bug from same log analysis**: `markSourceDeletedByMissingNsmNums` (`notice-archive.service.ts`) marked bills `source_deleted` purely because their noticeNum was absent from one NSM list-crawl scan — zero detail-page confirmation, unlike the other two detection paths. Fixed by adding an optional `confirmDeletion: (billNo) => Promise<boolean>` callback param; both call sites (`archive-sync-phase-executors.ts`, `crawling-scheduler-pending-support.ts`) now wire it to `deps.crawlingCoreService.probeNsmDeletedBillAlert`, so list-absence is only a candidate signal, never sufficient alone.

- **3rd bug from same log analysis**: `getActiveNsmBillsForProbe`'s "Priority 1" query (`content_bill_number IS NULL` pool) had no `take`/limit and was ordered `noticeNum DESC`, so when that pool size >= the probe batch limit, every single cron cycle probed the exact identical top-N rows forever (prod evidence: identical candidate list `[2220569, 2220565, 2220561, ...]` logged unchanged across 24+ hours) — the rest of the pool (and the "Priority 2" broader-pool fallback) never got a chance to run. Fixed by switching both Priority-1 and Priority-2 queries to `createQueryBuilder` with `.orderBy('RANDOM()').limit(...)` for eventual full-pool coverage.

- **4th bug (log-noise) from same area**: `getArchivedNullContentIdNums` (used by the "existing-pending-recompare" cron candidate selection in `crawling-scheduler-pending-support.ts`/`archive-sync-phase-executors.ts`/proposalReason retry) had no `lifecycleStatus` filter, so an already-confirmed-`source_deleted` NSM bill (still `contentId IS NULL`) kept being re-selected and re-captured (repeated `captureNsmDetailFull failed ... timeout` log spam) every cron cycle forever. Fixed by adding `lifecycleStatus: 'active'` to its WHERE clause. Note: `getNoticesWithMissingSnapshotArtifacts` and the probe/list-reconciliation queries already had this filter; `getArchivedNullContentIdNums` was the one gap.

- **5th bug (more log-noise), found on follow-up recheck**: the screenshot-backfill/requeue candidate queries in `notice-archive.service.ts` — `getNoticesWithMissingScreenshots` (PAL), `getNoticesWithMissingNsmScreenshots` (NSM), `getAllPalNoticesForScreenshotRequeue` — also lacked `lifecycleStatus: NOTICE_LIFECYCLE_STATUS.ACTIVE`. A confirmed-deleted bill never has a screenshot (its detail page is gone), so it kept getting re-queued into `ArchiveOrchestratorScreenshotCoordinator`'s drain loop every `backfillMissingScreenshots` bootstrap/cron run, producing repeated "Screenshot failed for notice N ... re-queued" noise forever. When auditing "already-deleted bills still get logged/reprocessed" complaints, check ALL `contentId IS NULL`/`Not(IsNull())` candidate-discovery queries in `notice-archive.service.ts` for a missing `lifecycleStatus` filter, not just the ones already touched — this class of bug has recurred multiple times across different candidate-selection queries.

- **6th bug + root-cause reconciliation (2026-09-03)**: `collectSummaryBackfillCandidates` (backs `getPendingSummaryPage`/`getPendingSummaryPageByOffset`/`getNotSupportedSummaryRecoveryPage`/`getUnavailableSummaryPage`) also had no `lifecycleStatus` filter on its archive-row lookup, so AI summary generation kept being attempted on already-deleted bills (Ollama call + logs). Fixed the same way (added `lifecycleStatus: NOTICE_LIFECYCLE_STATUS.ACTIVE` to that one query — covers all 4 entry points at once). **More importantly**: the real reason "already-deleted bills keep getting logged" kept recurring even after adding `lifecycleStatus='active'` filters everywhere is that historical rows marked `source_deleted` *before* the `202609020001` trigger fix never actually got their `lifecycle_status` column corrected (the diffchain event exists, but the raw column silently stayed 'active' forever, per the earlier immutable-trigger bug) — so those specific rows kept passing every `lifecycleStatus: 'active'` filter as if they were still active. Added a proper fix: `ChangeTrackingService.getNoticeNumsWithInvalidatedEvent()` (distinct noticeNums with an INVALIDATED event) + `NoticeArchiveService.reconcileStaleLifecycleStatuses()` (for those noticeNums, if the archive row still says 'active' but the diffchain's latest `lifecycleStatus` field says `source_deleted`/`renumbered`, persist the correction via `archiveRepository.update`). Wired into `ArchiveSyncService.runBootstrapPipeline` as an early `safeRun` step (before pending sync), so every app restart self-heals any lingering drift instead of needing a manual one-off script. Tests: `notice-archive.service.spec.ts` (`reconcileStaleLifecycleStatuses` describe block).

- `NoticeLifecycleStatus` (`notice-archive.entity.ts`) is managed via a centralized `NOTICE_LIFECYCLE_STATUS` const object (`{ ACTIVE, SOURCE_DELETED, RENUMBERED }` + derived union type), mirroring the existing `CHANGE_EVENT_TYPE` pattern — not a real TS `enum`, because lifecycleStatus flows through raw SQLite rows/diffchain JSON/many test literals, and a real string enum would force casts everywhere without adding safety. Use `NOTICE_LIFECYCLE_STATUS.ACTIVE` etc. in new application code instead of raw `'active'`/`'source_deleted'`/`'renumbered'` string literals; migration files intentionally keep raw SQL string literals (historical migrations shouldn't depend on mutable app-layer imports).

- `source-deletion-detection.e2e-spec.ts` (npm run test:e2e only, not default `npm test`) predates the archiveNsmBillItems double-confirmation fix: its `NsmBillDeletedError` tests must set `crawlingCoreService.probeNsmDeletedBillAlert` to resolve a non-null string before asserting `appendSourceDeletedEventByNoticeNum` was called — the provider's default mock is `null` (unconfirmed), so without an explicit override the deletion is correctly never marked and the assertion fails. Also removed the dead `markSourceDeletedByMissingPalNums` (no production caller, no `confirmDeletion` support) from this spec, its mocks, and the service.

- **7th bug (2026-09-03), mass chain-audit failure (7688 failures / 20487 notices, all `event_hash_mismatch`+`changed_field_count_mismatch`+`diff_summary_mismatch` starting at eventHeight=1)**: `verifyNoticeChain` (`change-tracking-chain-audit.utils.ts`) seeded `currentState` from the CURRENT live `notice_archives` row (`archiveSnapshotByNum`/`buildAuditSeedSnapshot`) for every field unconditionally, even though genesis event reconstruction forces `beforeState=null`. Any field whose real transition is recorded by a LATER event in the same chain (e.g. `lifecycleStatus`/`sourceDeletedAt` set at eventHeight=2 by a `source_deleted` event, especially after the immutable-trigger/`reconcileStaleLifecycleStatuses` fixes made the live row finally match that later event) leaked backward into the genesis (height 1) reconstruction as a phantom "already present" value, inflating changedFieldCount/diffSummary/eventHash for height 1 and cascading to every subsequent height. Fixed by computing `fieldsTouchedByChain` (union of `detail.fieldPath` across ALL of the chain's own recorded details) and excluding those fields from the initial seed — seed now only fills fields the chain's own history never records (true schema-added-later case, e.g. `contentId` before `canonVersion` existed), while chain-recorded fields always start unset/null and are rebuilt purely by replaying events in order. Verified by copying `lawcast.db` and running `runScheduledChainAudit('daily')` standalone: failureCount went from 7688 to 0 across all 20487 notices/46156 events. When debugging chain-audit mismatches that start at eventHeight=1 for notices with a `source_deleted`/status-changing history, suspect this seed-leak pattern first.


